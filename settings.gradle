rootProject.name = 'DA-Backend'

include 'server'
include 'core'
include 'eis'
include 'beispiel'
include 'diplomdb'
include 'bsvw'

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}